package cartes.util;

public interface EcouteurModele {
    /**
     * Methode permettant de mettre a jour le modele.
     * @param m Le modele a mettre a jour.
     */
    public void modeleMisAJour(Object source);
}
